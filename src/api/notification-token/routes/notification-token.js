'use strict';

/**
 * notification-token router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::notification-token.notification-token');
